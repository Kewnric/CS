Separates groups inside a card, settings sheet or toolbar.

```jsx
<Divider />
<Divider orientation="vertical" />
```

Inside a flex column the margin stacks on the gap — set margin:'0.25rem 0' there, as the admin form does.
