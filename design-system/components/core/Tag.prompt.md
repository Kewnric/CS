Removable chip for user-applied labels — topics on a program, filters on a list.

```jsx
<Tag onRemove={() => drop('pointers')}>pointers</Tag>
```

Lay runs of tags out with display:flex; gap:0.375rem; flex-wrap:wrap.
