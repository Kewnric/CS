Fills an empty pane or panel. The copy is encouraging and second-person, never an error.

```jsx
<EmptyState icon="scroll-text" title="No quests yet" description="Accept one from the board to start earning XP." />
```
