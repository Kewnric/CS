Difficulty rank on a program, snippet or quest. Tiny (10px/800, uppercase, wide tracking) and always to the right of a title.

```jsx
<TierBadge tier="S" />
<TierBadge tier="B" label="Tier B" />
```

The gradients are fixed per tier — never recolour them. S is the hardest.
