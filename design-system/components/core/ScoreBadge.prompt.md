Score readout in history tables and result dialogs.

```jsx
<ScoreBadge perfect>18 / 18</ScoreBadge>
<ScoreBadge>14 / 18</ScoreBadge>
```

There is no red state — a partial score is amber, never a failure.
