Single-line text entry. Always pair with a FormLabel.

```jsx
<Input placeholder="e.g. Reverse a linked list" />
<Input type="number" align="center" min={0} defaultValue={0} />
```

Number triples (hours/mins/secs in the session dialog) are centre-aligned in a flex row with gap 0.5rem.
