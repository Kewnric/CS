The library's folder tree. Nest TreeNodes as children and bump level each time.

```jsx
<TreeNode label="Arrays" kind="folder" level={0} expanded count={12} onToggle={t}>
  <TreeNode label="Bubble Sort" kind="item" level={1} active />
</TreeNode>
```
