Switches panes within one screen.

```jsx
<Tabs variant="underline" value={tab} onChange={setTab}
  items={[{label:'Programs',icon:'code-2'},{label:'Snippets',icon:'scissors'},{label:'Notes',icon:'notebook-pen'}]} />
```

Underline for the main pane switch; pill for a secondary scope filter inside a panel.
