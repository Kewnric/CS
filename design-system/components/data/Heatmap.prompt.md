Practice consistency over time. Lives in the wide home column inside a PanelCard.

```jsx
<PanelCard icon="calendar-days" title="Practice activity">
  <Heatmap weeks={26} />
</PanelCard>
```

Only indigo — the app never uses green for this grid.
